# ═══════════════════════════════════════════════════════════════════════════════
# FREIGHT OWNER QUERIES
# ═══════════════════════════════════════════════════════════════════════════════

extend type Query {
  getFreightOwner(id: ID!): FreightOwner
  listFreightOwners(input: PaginationInput!): FreightOwnerConnection!
}

type FreightOwnerConnection {
  items: [FreightOwner!]!
  pageInfo: PageInfo!
}
