/**
 * Employee Mutation Hooks
 *
 * Usage:
 * import { useCreateEmployee, useUpdateEmployee } from '@/entities/core/employee/api/mutations';
 *
 * const [createEmployee, { loading }] = useCreateEmployee();
 */

export * from "./hooks";
