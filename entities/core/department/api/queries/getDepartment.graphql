query GetDepartment($id: String!) {
  department(id: $id) {
    id
    companyId
    name
    headId
    head {
      id
      firstName
      lastName
      gradeId
    }
    createdAt
    updatedAt
  }
}
