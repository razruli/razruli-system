/**
 * Department Query Hooks
 *
 * Usage:
 * import { useGetDepartment, useGetDepartments } from '@/entities/core/department/api/queries';
 *
 * const { data, loading } = useGetDepartment({ id: '...' });
 */

export * from "./hooks";
