query Users($input: UsersInput!) {
  users(input: $input) {
    users {
      id
      email
      name
      status
      image
      createdAt
      updatedAt
    }
    pageInfo {
      total
      offset
      limit
    }
  }
}
